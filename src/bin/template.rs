#[aoc::day(01, "")]
#[aoc::asserts("0", "0")]
fn main(input: String, line_ending: &str) -> (usize, usize) {
    let _line_count = input.lines().count();
    let mut _parsed_data = input.split(line_ending);

    (0, 0)
}
