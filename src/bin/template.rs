#[aoc::day(01, "")]
#[aoc::asserts("", "")]
fn main(input: String, line_ending: &str) -> (usize, usize) {
    let line_count = input.lines().count();
    let mut parsed_data = input.split(line_ending);

    (0, 0)
}
